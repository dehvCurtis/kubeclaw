apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: openclaw-gateway
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: openclaw
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
