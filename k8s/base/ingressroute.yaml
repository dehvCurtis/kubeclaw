apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openclaw-gateway
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`kubeclaw.local`)
      kind: Rule
      services:
        - name: openclaw-gateway
          port: 18789
  tls:
    secretName: openclaw-tls
